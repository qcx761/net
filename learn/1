; 1. 应用层 (Application Layer)
; 功能：提供用户接口和应用程序服务。为用户的应用程序提供网络服务，负责应用程序与网络之间的交互。
; 协议：HTTP、FTP、SMTP、DNS等。
; 2. 表示层 (Presentation Layer)
; 功能：负责数据格式转换、数据加密和压缩。确保从应用层发来的数据能被接收方理解，可能涉及数据编码的转换。
; 协议：JPEG、MPEG、SSL/TLS等。
; 3. 会话层 (Session Layer)
; 功能：管理应用程序之间的会话，控制对话的建立、维护和终止。确保数据在会话中的顺序和完整。
; 协议：NetBIOS、RPC等。
; 4. 传输层 (Transport Layer)
; 功能：负责数据通信的可靠性和完整性，包括数据的分段、传输和重组。提供端到端的通信，流量控制和错误校正。
; 协议：TCP、UDP等。
; 5. 网络层 (Network Layer)
; 功能：负责数据包的转发和路由选择，处理设备之间的逻辑寻址。确保数据能够从源到目标的最佳路径传输。
; 协议：IP、ICMP、IGMP等。
; 6. 数据链路层 (Data Link Layer)
; 功能：在局域网内提供节点之间的数据传输。负责对数据帧的封装和解封装，以及介质访问控制。
; 协议：Ethernet、PPP、HDLC等。
; 7. 物理层 (Physical Layer)
; 功能：传输原始比特流，包括电缆、插头和电信号等物理媒介的标准。处理设备与传输介质的物理连接。
; 协议：RS-232、USB、Ethernet电气接口等。